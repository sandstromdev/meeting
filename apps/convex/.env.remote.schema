# Schema for Convex **dashboard** env (`convex env list`), validated by `scripts/validate-convex-env.ts`.
# Local CLI / `.env.local` uses `.env.schema` instead.

# @import("../../packages/common/.env.schema")

# --- Internal ---

# Value for `apps/convex` remote env validation (`.env.remote.schema` + validate-convex-env)
# @optional
# @example="true" @type=boolean
IS_CONVEX=
